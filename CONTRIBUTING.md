# 기여 안내

## 개발 흐름

1. `main`에서 수명이 짧은 작업 브랜치를 만듭니다.
2. 변경에 맞는 테스트를 먼저 추가하거나 수정합니다.
3. `make quality`와 `make scan`을 실행합니다.
4. Pull Request 또는 Merge Request 템플릿에 위험, 검증, 배포, 롤백 정보를 작성합니다.
5. CODEOWNERS로 담당 범위를 확인하고 필수 CI 검사를 통과한 뒤 보호된 `main`에 병합합니다.

정책, 스캐너 어댑터, 증적, 배포 코드를 변경할 때는 기존 PASS 입력이 실패하거나 FAIL 입력이 통과하지 않는지 반드시 확인합니다. 의도적으로 판정 기준을 바꿨다면 정책 버전을 올리고 변경 이유를 문서화합니다.

## 파서 추가

새 도구를 추가할 때는 원본 페이로드를 그대로 게이트에 넘기지 않습니다. `ScanResult`와 `Finding`으로 변환하고 다음 테스트를 포함합니다.

- 정상 보고서와 빈 보고서
- 스캐너 오류 또는 잘못된 형식
- 심각도 매핑
- 위치와 구성 요소 정보
- 같은 이슈를 식별하는 안정적인 지문(fingerprint)
- 입력 파일 SHA-256과 스캔 실행 증명서의 결속

## 보안

실제 비밀 값, 내부 레지스트리 주소, 고객 데이터, 운영 클러스터 정보는 테스트 데이터나 로그에 넣지 않습니다. 취약점은 공개 이슈 대신 [SECURITY.md](SECURITY.md)의 절차에 따라 제보합니다.
