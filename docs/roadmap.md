# 단계별 개선 이력과 로드맵

상태는 이 저장소의 코드와 테스트로 확인한 범위만 표시합니다. 외부 서버 연동이 필요한 항목은 인터페이스를 구현했더라도 완료로 적지 않았습니다.

## P0: 우회, 변조, 오배포 차단 — 완료

| 개선 | 완료 기준 | 구현 위치 |
| --- | --- | --- |
| 안전한 증적 생성 | FinGuard가 소유한 번들만 교체, 링크 입력 거부, 준비 디렉터리 작성 후 원자적 게시 | `finguard/evidence.py` |
| 릴리스 대상 결속 | 커밋, 이미지, SBOM, 배포 위치, 상태 확인 URL을 정규형 해시로 대조 | `finguard/release.py` |
| 승인 대상 결속 | 전체 변경 요청과 릴리스 대상 해시를 ITSM 서명 페이로드에 포함 | `finguard/approvals.py` |
| 배포 정책 결속 | 허용된 정책 ID, 버전, 원문 SHA-256이 증적에 기록된 정책과 모두 일치해야 배포 | `finguard/deployment.py`, 파이프라인 계약 테스트 |
| 평가 입력 고정 | 게이트, 검증, 배포 명령이 비공개 스냅샷의 동일한 바이트만 검증하고 사용 | `finguard/cli.py` |
| 단일 빌드 | 레지스트리 다이제스트를 SCA, DAST, 승인, 배포에 재사용 | GitLab CI, Jenkinsfile |
| CI 신뢰 분리 | MR 잡에 운영 키, 변경 승인, Kubernetes 자격 증명을 제공하지 않음 | 파이프라인 계약 테스트 |
| 부동 도구 이미지 | CI와 Dockerfile은 다이제스트 참조만 허용하고, 온프레미스 인프라는 다이제스트와 Cosign 서명을 실행 전에 검증 | `validate-images`, 운영 스크립트와 테스트 |
| 안전한 정리 | `make clean` 범위를 저장소의 `build/`로 고정 | `scripts/clean_build.py` |
| 재사용 러너 오염 차단 | Jenkins가 체크아웃 전에 전용 작업 공간을 비워 이전 보고서, 증명서, Git이 추적하지 않는 소스를 제거 | `Jenkinsfile`, 파이프라인 계약 테스트 |

## P1: 판정 정확성과 감사 통제 — 완료

| 개선 | 완료 기준 | 구현 위치 |
| --- | --- | --- |
| 스캔 실행 증명서 | 보고서, 스캐너, 소스, 아티팩트, 규칙 세트, 허용된 명령어 해시, 종료 코드, 취약점 DB, CI 실행 정보, 서명, 최신성 검증 | `finguard/attestation.py` |
| 파서 정확성 | 엄격한 JSON 파싱, 스키마와 필수 필드, ZAP 인스턴스, 중첩 CycloneDX 구성 요소와 CVSS 교차 검증, 범용 SARIF, XML 집계값과 DTD 검증 | `finguard/parsers/` |
| 스캐너 간 중복 제거 | 도구명과 메시지를 지문에서 제외, 경로 대소문자와 라이선스 버전 구분, 확인된 최고 심각도와 `observed_by` 보존 | `finguard/models.py`, `finguard/gate.py` |
| 정책 모호성 차단 | 심각도 별칭, 승인 역할, 라이선스 식별자의 의미상 중복과 상충되는 분류를 정책 로딩 시 거부 | `finguard/config.py` |
| SPDX 표현식 | `AND`, `OR`, `WITH`, 괄호를 평가하고 오류가 있으면 차단 | `finguard/licenses.py` |
| 예외 관리 | 심각도, 범주, 정책, 서비스, 환경, 기간, 연장, 취소, 보상 통제 | `finguard/config.py` |
| VEX | 별도로 서명한 증적의 대상, 발급자, 키, 유효 기간, 근거만 탐지 결과 억제에 사용 | `finguard/vex.py` |
| 테스트 지표 | 여러 커버리지 결과로 인한 모호성, 테스트 0건, 선언 건수와 실제 테스트 케이스 수의 불일치 차단 | 강화 테스트 |
| 배포 증적 유효성 | 미래 시각이거나 정책의 최대 나이를 넘긴 PASS 증적의 재사용 차단 | 배포 회귀 테스트 |
| 배포 감사 서명 | 실제 배포 결과의 Cosign 서명 강제, 기존 결과 덮어쓰기 기본 금지, 완성된 번들만 원자적으로 게시 | 서명 및 배포 회귀 테스트 |
| 공개 CI 판정 경계 | 테스트 실패와 Semgrep 탐지는 보고서로 보존하고, 스캐너 자체 오류와 정책 위반을 구분 | GitHub Actions, 종료 코드 계약 테스트 |
| 공개 저장소 통제 | PR과 필수 CI 검사 없이 `main`을 변경할 수 없고, 강제 푸시와 삭제를 차단 | GitHub Ruleset, CODEOWNERS, 공개 PR 이력 |
| 의존성 갱신 | Python과 GitHub Actions 의존성의 정기 업데이트 및 보안 업데이트 제안 | Dependabot, GitHub 취약점 알림 |

## 외부 승인과 서명 경계 — 완료

- ITSM 승인 증명서를 전체 `ChangeRequest` 해시, `ReleaseSubject` 해시, 발급 시각에 결속했습니다.
- 운영 릴리스 정책은 허용된 ITSM 발급자, Cosign 서명 방식, 키 ID만 인정합니다.
- 외부 승인 개인키를 CI에 주입하지 않고 공개키로만 검증합니다.
- 최종 증적도 KMS 또는 Vault URI를 사용한 Cosign으로 서명하고, 배포 러너는 공개키만 가집니다.

## 개발자 피드백과 정책 비교 — 완료

- GitLab Code Quality 및 SARIF 내보내기 결과를 제공합니다.
- 기준 정책과 후보 정책에서 발생한 위반 및 지문의 변화를 비교합니다.
- `--shadow-policy`는 기존 판정을 바꾸지 않고 후보 정책 결과를 증적에 함께 보존합니다.
- MR은 운영 승인을 요구하지 않는 전용 정책으로 실패 원인을 빠르게 보여줍니다.

## 배포 안전성과 운영 지표 — 완료

- Kubernetes 컨텍스트와 네임스페이스를 명시하고 `auth can-i` 사전 권한 검사를 통과한 경우에만 배포를 진행합니다.
- 승인된 상태 확인 URL을 대상으로 재시도 가능한 스모크 테스트를 실행합니다. 실제 배포 결과의 서명을 강제하며, 롤아웃, 스모크 테스트, 결과 서명 중 하나라도 실패하면 정확한 이전 이미지와 감사 애너테이션을 복원합니다.
- 게이트, 심각도, 예외, VEX, OSS 인벤토리, 승인 증적 검증 지표를 Prometheus 형식으로 생성합니다.
- GitLab DAST 네트워크와 컨테이너 이름을 파이프라인 및 잡 ID별로 분리했습니다.

## 다음 우선순위

### P2: 실제 외부 시스템 통합

1. ITSM API와 IdP 그룹을 연결해 승인자의 현재 직무를 검증합니다.
2. 현재 각 스캐너 잡이 직접 발급하는 HMAC 증명서를 플랫폼 워크로드 아이덴티티와 스캐너별 단기 키로 교체합니다.
3. 정책 번들과 스캐너 규칙 세트를 별도 공급망 증적으로 서명하고 해시 허용 목록을 자동으로 관리합니다.
4. Dependency-Track, FOSSA 또는 사내 OSS API에 SBOM을 전송하고 법무 승인 상태를 조회합니다.

### P2: 공급망 재현성

1. `requirements-dev.lock`에 포함된 정확한 버전을 아티팩트 해시와 함께 관리하고 `--require-hashes`를 적용합니다.
2. 폐쇄망 패키지 미러 스냅샷과 컨테이너 반입 목록을 릴리스 증적에 추가합니다.
3. OCI 레지스트리에 SLSA 출처 증명, SBOM, VEX, 판정 결과를 참조 아티팩트로 게시합니다.

### P3: 운영 플랫폼

1. 증적을 WORM 또는 객체 잠금 저장소로 자동 이관하고 색인합니다.
2. Prometheus Pushgateway와 SIEM 어댑터를 추가하고 SLO와 경보 기준을 정의합니다.
3. GitLab과 Jenkins를 함께 운영하는 경우를 위한 외부 분산 배포 잠금을 도입합니다.
4. 비가역 데이터베이스 변경을 위한 expand-contract 검증과 연습 환경 복구 테스트를 추가합니다.

## 완료 판정 기준

다음 작업은 코드가 존재한다는 이유만으로 완료 처리하지 않습니다. 실제 외부 시스템의 신뢰 경계를 통과하는 통합 테스트와 키 회전, 장애 대응 사례, 런북, 감사 로그까지 확인해야 완료입니다.
